# ClimbAssist AI - Complete Local Requirements
# Contains all features for local machine deployment
# For Streamlit Cloud, use: requirements-cloud.txt

# Web App Framework
streamlit>=1.28.0

# Core ML & Vision
numpy>=1.23.0
scipy>=1.10.0
Pillow>=9.5.0
scikit-image>=0.21.0

# PyTorch (for route planning & segmentation)
# These are optional - app works without them, just with reduced features
torch>=2.0.0 ; sys_platform != 'win32' or python_version < '3.12'
torchvision>=0.15.0 ; sys_platform != 'win32' or python_version < '3.12'
segmentation-models-pytorch>=0.3.3 ; sys_platform != 'win32' or python_version < '3.12'
timm>=0.9.0 ; sys_platform != 'win32' or python_version < '3.12'

# OpenCV (for video processing)
opencv-python>=4.8.0

# TensorFlow (for MoveNet pose estimation)
tensorflow>=2.13.0
tensorflow-hub>=0.13.0

# PDF Generation
fpdf2>=2.7.0

# Utilities
scikit-learn>=1.3.0
networkx>=3.1
matplotlib>=3.7.0
requests>=2.31.0
